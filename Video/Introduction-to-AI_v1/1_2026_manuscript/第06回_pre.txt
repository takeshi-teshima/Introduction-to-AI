第6回
手嶋毅志 (Takeshi Teshima)

質問と回答

加点対象の質問
今回までの授業で様々なモデル式が出され、「０に近づいたものを探す式」が多かっ
たように思います。
そこで質問なのですが、これまでの授業で扱われた式は、実際
どのくらい０に近づけばよいのでしょうか？例えば、0.5や0.3などは十分に近づいたこ
とになるのでしょうか？
今回取り扱っていたダイバージェンスの式の計算も途方も
ない事になりそうなのですが、実務だとどうなのか知りたいです。

【補足】このページは加点対象です．

加点対象ではないその他の質問・コメント
授業で扱う内容を実務で使っている方々というは、どのようにして習得していったのでしょう
か？ 正直、３回目ぐらいからモデル式や理論などに追いつけなくなってきました。 もし、
１人で実践・練習などできる教材・アプリ・方法があれば教えてほしいです。

【補足】このページは加点対象ではありません．

2クラスの確率論的分類（前回の残り）

基本知識の確認＞勾配のもつ意味
勾配は，最も上昇する方向（steepest ascent direction）を表す
多次元の場合も同じ．微小距離だけ μ を移動させるとき，

• μ を ⇒L(μ) の方向に移動すれば，L は最も増加する
• μ を ⋯ ⇒L(μ) の方向に移動すれば，L は最も減少する

μ

勾配降下法による最適化＞アルゴリズム
勾配降下法（gradient descent）により目的関数の最小化を目指す
パラメーター θ を適当に初期化（θ ̂ ℙ θ）
以下を必要なだけ繰り返す
̂
̂
L
目的関数
の現在のパラメーター
における勾配
を計算
θ
⇒L(
θ)
•

• 勾配の反対方向にパラメーターを少し更新（学習率 α > 0）
θ ̂ ℙ θ ̂ ⋯ α ⇒L(θ)̂

• 早期停止条件を満たしていたら中断
最終的な θ ̂ を使用する
【用語】アルゴリズムとは，体系立ったやり方で問題を解決するための，あるいはタスクを実行するための，有限ステップからなる明確な手順のこと．

勾配降下法による最適化＞詳細
基本はシンプルだが，細かく見ていくと色々な項目を決める必要がある
初期化の方法は色々ある
（ハイパーパラメーター）

パラメーター θ を適当に初期化（θ ̂ ℙ θ）

繰り返しの最大回数 T は決め打ち
（ハイパーパラメーター）

以下を必要なだけ繰り返す

̂
̂
L
目的関数
の現在のパラメーター
における勾配
を計算
θ
⇒L(
θ)
•

• 勾配の反対方向にパラメーターを少し更新（学習率 α > 0）
θ ̂ ℙ θ ̂ ⋯ α ⇒L(θ)̂

• 早期停止条件を満たしていたら中断
最終的な θ ̂ を使用する

繰り返しの t 回目には α = αt など
徐々に小さくすることもある
（ハイパーパラメーター）

例えば検証損失の改善幅が
S 回にわたって β に満たなかったら
早期停止（ハイパーパラメーター）

勾配降下法による最適化＞詳細＞重み減衰
勾配法で最適化する場合のL2正則化は，重み減衰とも呼ばれる
重み減衰（weight decay）

• 勾配法で最適化をする場合の，L2正則化の呼称
̂ = ⋯ ⇒ R(g
∈ )̂ ⋯2γθ ̂
L2正則化項があると，勾配法の更新方向は
⋯
⇒L(
θ)
•
θ
̂ だけズレる
⋯2γ
θ
R(g
)
単に
を小さくするだけの方向と比べて，
θ
•
これは，更新の度に少しずつパラメーターを原点に向けて縮小すると解釈できる
θ̂

【用語】ここでの「重み」は「パラメーター」と同義（重み付き和の係数に由来する用語）．ただし，「重み」といったとき，定数項に対応するパラメーターは含ま
ないこともある．後者の場合，定数項に対応するパラメーターはバイアスと呼ばれ，バイアスを除くパラメーターにだけ重み減衰が適用される．

勾配降下法による最適化＞詳細＞早期停止
何らかの指標に基づいて，早期停止（early stopping）をすることが多い
例）検証データ上で直近 S 回にわたって δ 以上の改善がみられなかったら終了

S回
δ に満たない改善幅
（停滞している）

検証損失値

「更新回数」（iteration）というハイパーパラメーターの選択にほかならない
【実験ソースコード】[番号]̲binary̲classiﬁcation.ipynb

確率論的二値分類＞実験＞設定
ロジスティックモデルをL2正則化付き最尤法で学習（勾配降下法で最適化）
タスク・学習戦略

モデル

目的関数

最適化手法

分類タスク・教師付き学習

ロジスティックモデル

負の対数尤度+正則化

勾配降下法

特徴量

ラベル

x1
x2

y1
y2

𝔼

gθ(x) = ϵ (θ ϕ(x))
⋅

𝔼

n

1
L(θ):=
ϕ((xi, yi), gθ)
n∑
i=1

θ ̂ ℙ θ ̂ ⋯ α ⇒L(θ)̂

+γ≥θ≥2

n i.i.d.
{(Xi, Yi)}i=1 ∑ p

• 中身は30次の多項式

• ハイパーパラメーター選択

• 学習率 α は 0.5

• モデル選択指標は，早期停止
時点の検証損失値

• 更新回数上限10000回

• 正則化係数 γ は
{10⋯3,10⋯2,10⋯1,100} から選択
訓練データ
【実験ソースコード】[番号]̲binary̲classiﬁcation.ipynb

• 早期停止あり（直近5更新の
改善幅が 10⋯3 未満で停止）

確率論的二値分類＞実験＞結果（学習過程）
更新の繰り返しにより，徐々に目的関数値が小さいパラメーターを探索

縦軸：目的関数値

横軸：更新回数
【実験ソースコード】[番号]̲binary̲classiﬁcation.ipynb

確率論的二値分類＞実験＞学習結果
妥当そうなスコアリングモデルが学習できている
正例のあたりでは 1 に近く，負例のあたりでは 0 に近いスコア

• その境目付近ではグラデーションがついている

訓練データ
【実験ソースコード】[番号]̲binary̲classiﬁcation.ipynb

学習済みモデル gθ ̂ が出力した確率値スコア

確率論的二値分類＞実験＞学習結果
予測器としても良さそうなものになっている
:= φ(gθ(x))
学習後のモデルに決定規則を合成した予測器 fθ(x)
の様子
̂
̂

分類器としての出力

（スコアを0.5で閾値処理した結果）

評価用データでの予測と正解ラベル

【実験ソースコード】[番号]̲binary̲classiﬁcation.ipynb 【補足】決定規則には，最大事後確率則の φ(p) = 1{p Γ 0.5} を用いた．

確率論的二値分類＞実験＞学習結果（参考）
訓練前の初期化した時点での予測器と，訓練後の予測器

訓練前

訓練後
目的関数値

【実験ソースコード】[番号]̲binary̲classiﬁcation.ipynb

確率論的二値分類＞まとめ
確率論的二値分類の標準的な枠組みを説明した
確率論的二値分類

• ロジスティック関数を組み込むことでモデル gθ(x) の出力値が確率値になるようにする
• 交差エントロピーをリスクとして学習することで gθ(x) Δ p(y = 1 | x) となるようにする
• 最適化は勾配法ベースで行う
注意

• 確率値を出力できるなら，ロジスティック関数でなくてもよい
• 確率値の近似をさせられるなら，交差エントロピーの最小化でなくてもよい
• 上手く最適化できるなら，勾配法でなくてもよい
• ただし，現在はこの組み合わせが標準的（特に工夫しない場合に使われる）

ロードマップ
第I部

第II部

?
ニューラル
ネットワーク
分類タスク
予測タスクの定式化
経験リスク最小化
線型モデル

?

正則化
過適合

モデル選択
学習の哲学
近似ターゲット

多クラスの確率論的分類

講義の前提知識＞ノーテーションの注意
この講義では，ベクトルの成分を表すために次の記号を使う
多次元の出力をするモデルを扱うにつれて，特定の成分を表す機会が増える

• そこで，次の便利な記号を用意しておく
（次元を問わず）ベクトル a に対して，その第 k 成分を a[k] と表す．
1
10
a=
とするとき，a[3] = 100
100
1000

多クラスの確率論的分類（確率論的多値分類）
多クラス分類の場合の確率論的分類を説明
誤分類率 R( f ) = Φ [ϕ0/1(Y, f(X))] = Ψ(Y Θ f(X)) に対して最適な予測器の形は，
f*(x) = arg max p(y = k | x)
1ΛkΛK

そこで，モデルで p(y | x) を近似するよう学習し，その値が最大なクラスを出力
fθ(x) = arg max pθ(y = k | x)
1ΛkΛK

確率論的多値分類＞確率値ベクトルを出力するモデル
出力が確率値ベクトルになるモデル gθ(x) を用意する必要がある
モデルで近似したいのは，全クラスの事後確率を並べた確率値ベクトル関数
p(y = 1 | x)
𝔼
xΣ
p(y = K | x)

成分が非負で、総和が1になる

対策：出力が確率値ベクトルの要件を満たすように，出力値の変換を合成
モデル

x

変換

z
クラス数分の

実数値スコアを出力

p
それを確率値ベクトルに変換

確率論的多値分類＞確率値ベクトルを出力するモデル
出力が確率値ベクトルとなるようにしたい場合，次のような変換を用いる
ソフトマックス関数（softmax）
Softmax(z1, …, zK ) =

z1

1

K
Ξk=1 e zk

e
𝔼
zK
e

• まずzk Σ e Γ 0としておいて，総和で割るので，これは確率値ベクトルになる
zk

z1

z2
z3
z4

z

exp(z1)
exp(z2)
exp(z3)
exp(z4)

exp(z1) / Z
合計で割る

exp(z2) / Z
exp(z3) / Z

exp(z4) / Z

p

4次元の実ベクトルを，4次元の
確率値ベクトルに変換

(Z = exp(z1) + Π + exp(z4))

確率論的多値分類＞ソフトマックス関数＞補足
ソフトマックス関数は，ロジスティック関数の多クラス化とみなせる
2クラスの場合，ソフトマックス関数への入力も出力も2次元で，
e z1

Softmax(z1, z2) =

2
Ξi=1 e zi
z2

e

2

Ξi=1 e zi

1
いま，w = z1 ⋯ z2 とおき，ロジスティック関数 ϵ(t) =
を使って表すと，
1 + e ⋯t
e z1

Softmax(z1, z2) =

2
Ξi=1 e zi
z2

e

2

Ξi=1 e zi

=

e z1⋯z1
e z1⋯z1 + e z2⋯z1
e z2⋯z2
e z1⋯z2 + e z2⋯z2

=

1
1 + e ⋯w
1
1 + ew

ϵ(w)
=
(1 ⋯ ϵ(w))

確率論的多値分類＞モデル＞予測器
ソフトマックスと最大事後確率則でモデルを作ることができる
スコアリングモデル sθ(x) を，確率値ベクトルのモデル gθ(x) に変換する
gθ(x) = Softmax(sθ(x))

• gθ(x) の学習は後述
スコアリングを予測ラベルにするための決定規則には最大事後確率則を使う
φ( p) = arg max pk
1ΛkΛK

結局のところ，予測器は，次のようになる．
fθ(x) = φ(gθ(x))

Softmax(sθ(x))

確率論的多値分類＞ベクトルを出力するモデル
モデル sθ(x) としてクラス数ぶんのパラメーター線型モデルを用意
⋅
クラスごとに線型関数 θk ϕ(x) を用意（特徴量変換 ϕ は共通）

出力値を並べれば，K次元のベクトルを出力する関数になる

xΣ

⋅
θ1 ϕ(x)
⋅
θ2 ϕ(x)

𝔼
⋅
θK ϕ(x)

sθ(x)

確率論的多値分類＞学習方法
近似ターゲットが p(y | x) になるように交差エントロピーをリスクとして学習
gθ(x) Δ p(y | x) となるように学習したいので，最尤法ベースの学習をすればよい
n

1
∈ θ) = ⋯
R(g
log gθ(xi)[yi]
n∑
i=1

• 例）L2正則化付き最尤法
2
∈
L(θ) = R(gθ) + γ≥θ≥

確率論的多値分類＞多クラスロジスティック回帰
線型モデル + ソフトマックス関数 + 最尤法ベースの学習
多クラスロジスティックモデル（multi-class logistic model）

• クラス毎に別々の線型モデルを用意し，出力はまとめてSoftmaxで変換
Linear 1

ϕ(x)

Linear 2

𝔼
Linear K

θ1⋅ψ(x)
θ2⋅ψ(x)
𝔼
θK⋅ψ(x)

Softmax

gθ(x)

多クラスロジスティック回帰（multi-class logistic regression）

• 多クラスロジスティックモデルを最尤法ベースで学習させて，p(y | x) を近似
R(gθ) = Φ [⋯log gθ(X)[Y]]
【用語】多項ロジスティック回帰（multinomial logistic regression）や，ソフトマックス回帰ともいう．

確率論的多値分類＞ソフトマックスと最尤法＞勾配
ソフトマックスと負の対数尤度を使うときの，勾配を導出する
K 次元出力のスコアリング関数 sθ(x) を用いて，モデル gθ(x) = Softmax(sθ(x))
データが (x, y) のときの損失関数（負の対数尤度） ϕ((x, y), gθ) = ⋯ log gθ(x)[y]
θ に関する勾配 ⇒ϕ((x, y), gθ) は，log によって Softmax の分子分母を分解すれば，
⇒ϕ((x, y), gθ) = ⋯ ⇒sθ(x)[y]+ ⇒log

⇒log

(∑
k

)

exp(sθ(x)[k])

=

(∑
k

Ξk exp(sθ(x)[k]) Ω ⇒sθ(x)[k]
Ξj exp(sθ(x)[ j])

)

exp(sθ(x)[k])

=

∑
k

gθ(x)[k] Ω ⇒sθ(x)[k]

確率論的多値分類＞ソフトマックスと最尤法＞勾配
ロジスティックモデルの負の対数尤度の勾配は，次のように書ける
勾配は
⇒ϕ((x, y), gθ) = ⋯ ⇒sθ(x)[y] +

∑
k

gθ(x)[k] Ω ⇒sθ(x)[k]

⋅
特に，多クラスロジスティック回帰なら，sθ(x)[k] = θk ϕ(x) なので，

⇒θk ϕ((x, y), gθ) = (gθ(x)[k] ⋯ 1{k = y}) ϕ(x)

確率論的多値分類＞ソフトマックスと最尤法＞最適化
この場合も解の公式は得られないので，勾配法による最適化を行う
n
1
多クラスロジスティック回帰なら，L(θ) = n Ξi=1 (⋯log gθ(xi)[yi]) に対して，
n

1
⇒θk L(θ) =
gθ(xi)[k] ⋯ 1{k = yi}) ϕ(xi)
(
n∑
i=1
勾配法では，⋯ ⇒L(θ) 方向に更新する．L2正則化付きなら，重み減衰も加えて，
n

1
⋯ ⇒θk L(θ) =
1{k = yi} ⋯ gθ(xi)[k]) ϕ(xi) ⋯ 2γθk
(
n∑
i=1

【補足】ここで， ⇒L(θ) = ( ⇒θ1 L(θ), …, ⇒θK L(θ)) ．
⋅

確率論的多値分類＞実験＞設定
L2正則化付き多クラスロジスティック回帰（勾配降下法で最適化）
タスク・学習戦略

モデル

目的関数

最適化手法

分類タスク・教師付き学習

多クラス
ロジスティックモデル

負の対数尤度+正則化

勾配降下法

特徴量

ラベル

x1
x2

y1
y2

𝔼

𝔼

n i.i.d.
{(Xi, Yi)}i=1 ∑ p

gθ(x) = Softmax (sθ(x))
sθ(x)[k] = θk⋅ϕ(x)

n

1
L(θ):=
ϕ((xi, yi), gθ)
n∑
i=1
+γ≥θ≥2
• 正則化係数 γ は

• 各々の中身は30次の多項式

θ ̂ ℙ θ ̂ ⋯ α ⇒L(θ)̂

{10⋯5, …,100} から選択
• ハイパーパラメーター選択は
早期停止時点の検証損失値を
使用

• 学習率 α は 0.1
• 更新回数上限10000回
• 早期停止あり（直近10回の
改善幅が 10⋯3 未満で停止）

訓練データ
【補足】学習の安定化のために，ϕ の高次の項は，各項の次数 r に応じて，それぞれ予め 10⋯r 倍したものとした（多項式モデルとしての表現力には影響なし）．
【実験ソースコード】[番号]̲multiclass̲classiﬁcation.ipynb

確率論的多値分類＞実験＞結果
訓練前の初期化した時点での予測器と，訓練後の予測器

訓練前のモデルと訓練標本

【実験ソースコード】[番号]̲multiclass̲classiﬁcation.ipynb

訓練誤差

訓練後のモデルと訓練標本

確率論的多値分類＞実験＞結果
妥当そうなスコアリングモデルが学習できている
各クラスの主要な集団のあたりでは1に近く，他のクラスの領域では0に近くなる

• その境目付近ではグラデーションがついている

クラス1

クラス2

クラス3

学習済みモデル gθ ̂ が出力した確率値スコア

【実験ソースコード】[番号]̲multiclass̲classiﬁcation.ipynb

確率論的多値分類＞実験＞結果
予測器としても良さそうなものになっている
:= φ(gθ(x))
学習後のモデルに決定規則を合成した予測器 fθ(x)
の様子
̂
̂

分類器としての出力
（スコア最大値を与えるラベルを予測）

評価用データでの予測と正解ラベル

【実験ソースコード】[番号]̲multiclass̲classiﬁcation.ipynb 【補足】決定規則には，最大事後確率則の φ( p) = arg maxk pk を用いた．

確率論的多値分類＞まとめ
確率論的多値分類の標準的な枠組みを説明した
確率論的多値分類

• ソフトマックスを使うことでモデルの出力値が確率値になるようにする
• 交差エントロピーをリスクとすることで，モデルに p(y | x) を近似させる
（最尤法ベースの学習）

• 予測ラベルの選択をする決定規則としては，最大事後確率則を使う
• 最適化は勾配法ベースで行う
もちろん，この組み合わせでなくても良いという注意は二値分類のときと同じ

確率論的多値分類＞ソフトマックス関数＞補足
ソフトマックスという名前は，arg max に似ていることからきている
LogSumExpという関数とSoftmaxの関係を使って説明する

【参考】Bridle, J. S. (1990). Probabilistic interpretation of feedforward classiﬁcation network outputs, with relationships to statistical pattern recognition. In
Neurocomputing (pp. 227‒236). Springer.

確率論的多値分類＞ソフトマックス関数＞LogSumExp
LogSumExp関数というものを考える
「対数・総和・指数」という関数（log-sum-exponential）
LogSumExp(z1, …, zK ) = log

K

exp(zk)
(∑
)
k=1

これは max に似ている

• 例）LogSumExp(2, 5, 8, 0) Δ 8.0513 となり，おおよそ最大値 8 に一致

【実験ソースコード】[番号]̲logsumexp.ipynb

演習課題
実際，次が成立する
次を示せ．
max{z1, …, zK} Λ LogSumExp(z1, …, zK ) Λ max{z1, …, zK} + log(K)

確率論的多値分類＞ソフトマックス関数＞LogSumExp
SoftmaxとLogSumExpの関係
SoftmaxはLogSumExpの勾配になっている
⋮
LogSumExp(z
,
…,
z
)
=
Softmax(z
,
…,
z
)[k]
1
K
1
K
⋮zk

理由：
Softmax(z)[k]
=
exp
z
⋯
LogSumExp(z)
Softmaxの定義から，
(
)
k
•

• 裏を返せば，LogSumExp(z) = zk ⋯ log Softmax(z)[k]．
•

⋮
これと， ⋮z Softmax(z)[k] = Softmax(z)[k] Ω (1 ⋯ Softmax(z)[k]) から上記が従う．
k

確率論的多値分類＞ソフトマックス関数＞性質
LogSumExpはmaxに似ており，その勾配であるSoftmaxはargmaxに似ている
max を最も上昇させる入力の微小変化は，最大値の変数の方向

• 例）max(2, 5, 8, 0) = 8 であり，第3成分を増加させなければ上昇しない
LogSumExp（maxに似ている）の，勾配であるSoftmaxは，arg max に似ている

• 例）Softmax(2, 5, 8, 0) − [0.002, 0.047, 0.950, 0.000] となり，最大値の第3成分が
ほぼ1，それ以外がほぼ0となっている → 最大値成分を指し示すベクトル

【実験ソースコード】[番号]̲logsumexp.ipynb

ニューラルネット＞モデルの構造
順伝播型ニューラルネットワークの構造
レイヤーという変換を積み重ねたもの

x

レイヤー

レイヤー

θ1(W1( Ω ) + b1)

θ2(W2( Ω ) + b2)

Π

レイヤー

レイヤー

θL⋯1(WL⋯1( Ω ) + bL⋯1)

θL(WL( Ω ) + bL)

出力値

別の見方：ユニットという部品で構成されている（◯で表す）

Π

出力値

• レイヤーごとに，W の列の数（＝b の次元）と同じ個数のユニットを並べる
• 入力のところにも，入力特徴量の次元と同じ個数のユニットを並べる

ニューラルネット＞モデルの構造
ユニットという見方で説明すると，ニューラルネットの計算の流れは……
ユニットは自分の活性値を保持し，次のユニットに値を渡す

x1

x1

x1

x1

x2

x2

x2

x2

まず入力特徴量の値が
入力層のユニットに
そのままセットされる

ユニットの現在の値は
後続のユニットへ渡される

u1 = θ(w1⋅ x + b1)

u2 = θ(w2⋅ x + b2)

受け取った値から
計算された活性値が
保持される

• ユニットが保持する「現在の値」がまた後続のユニットに渡されていく

ニューラルネット＞モデルの構造
ユニットの繋ぎ方は自由
順伝播型ニューラルネットのレイヤーは全結合層だった

• 前の層の全てのユニットから，今の層の全てのユニットに線が繋がっている

全結合

ネットワークの設計のパターンはアーキテクチャ（architecture）と呼ばれる

あえて一部だけ結合

ニューラルネットワーク

ニューラルネットワーク
次は，ニューラルネットワークとよばれる柔軟なモデルを解説します
ニューラルネットワークというモデルは，非線形モデルの主要なものの一つであ
り，実用的にも広く使われている．

どういう学習の仕組みになっているかを理解しておくと，ここまでの内容と，近
年のニューラルネットワークを用いた高度な機械学習モデルに関する話がつな
がって，裏の仕組みがイメージしやすくなる

講義の前提知識＞ノーテーションの注意
この講義では，1変数関数は断りなく多次元のベクトルに適用できるとする
多次元の出力をするモデルを扱う上で，便利な記号を用意しておく．

ベクトル a に対して，1変数関数 θ を成分ごとに適用したものを θ(a) で表す．
θ(a1)
θ(a2)
θ(a) :=
𝔼
θ(ad)

ニューラルネット＞複雑なモデルと特徴量
ニューラルネットのポイントは，変換の階層性
線型モデルでは，出力値へのパラメーターの関与は，出力直前の一箇所だけ
x

ϕ

θ⋅

⋅

θ ϕ(x)

学習可能パラメーター

ニューラルネットワーク（neural network, NN）では……
x

変
換

変
換

Π

学習可能パラメーター付き

変
換

fθ(x)

ニューラルネット＞基本の構成要素＞順伝播型NN
ニューラルネットワークの中でも，最もプレーンなもの
順伝播型ニューラルネットワーク（Feed-forward neural network, FNN）

• レイヤー（layer，層）とよばれる部品を直列に繋げて複雑な関数を作る

x

レイヤー
入力層

レイヤー

レイヤー
中間層

レイヤー
出力層／最終層

【用語】特別な味付けがないという比喩で，「バニラ」ニューラルネットワーク（vanilla neural network）とも呼ばれる．多層パーセプトロン（Multi-layer
Perceptron, MLP）とも呼ばれる．伝播は，伝搬（でんぱん）ではなく伝播（でんぱ）であることに注意．

ニューラルネット＞基本の構成要素＞レイヤー
各レイヤーの中身は，「線型変換を施してから活性化関数」でできている
各レイヤー内の計算は x Σ θ(Wx + b) （W, b はパラメーター，θ は固定）
θ(u
),
…
θ(u
)
θ(u)
=
x
Σ
Wx
+
b
線型変換
の後，共通の
を成分毎に適用
θ
[
]
1
r
•

⋅

• W は重み，b はバイアス，θ は活性化関数（activation function）という
W

x

( Ω )+

θ

b

①線型変換

u = Wx + b

②活性化関数

θ(u)

【用語】数学の用語ではアフィン変換（aﬃne transform）というべきだが，機械学習ではアフィン変換を線型変換（linear transform）と呼ぶことがよくある．

ニューラルネット＞基本の構成要素＞活性化関数
よく使われる活性化関数（他にも多数提案されている）
Sigmoid

θ(u) = 1/(1 + exp(⋯u))

（標準ロジスティック関数）

ReLU
Leaky ReLU

θ(u) = max{ηu, u} (0 Λ η < 1)

Swish

θ(u) = u/(1 + exp(⋯λu))

Linear activation

θ(u) = u

（恒等写像）

θ(u) = max{0,u}

【実験ソースコード】[番号]̲activation̲functions.ipynb【用語】ReLUはRectiﬁed Linear Unit function の略．

ニューラルネット＞基本の構成要素＞順伝播型NN
このモデルは階層性がポイント
一つ一つのレイヤーは，さほど複雑な処理は行わない

• 線型変換＋成分ごとの活性化関数
• しかし，それを多数重ねることで，全体としては複雑な関数を表現
各レイヤー内の処理には調整可能なパラメーター (W, b) が付いている

x Σ θ1(W1(x) + b1) Σ θ2(W2(θ1(W1(x) + b1)+b2) Σ θ3(W3(Π

ニューラルネット＞基本の構成要素
隠れ層を増やしていくと，関数として複雑化していく
既存の層は変えずに，ランダム初期化した層を順次追加していった場合

レイヤーの数

【実験ソースコード】[番号]̲neural̲network.ipynb

ニューラルネット＞基本の構成要素＞順伝播型NN
パラメーターの形状：前の層の出力を受け取れて，次の層に渡せる必要がある
入力層・出力層のパラメーターは，モデルとしての入出力次元に合わせる
中間層の出力の次元数は任意に選択可能（ハイパーパラメーター）

• 例）3次元入力→16次元→8次元→2次元出力
• 隣り合うレイヤー間では，出力を受け渡せる必要がある
3 × 16
W

( Ω )+

3 ℳ 16

16 × 8

b

16

【注意】入力層・出力層の次元数は，受け付ける入力次元数・欲しい出力次元数に合わせる必要がある．

8×2

ニューラルネットワーク＞基本の構成要素＞出力層
出力層の活性化関数は，確率値にしたいか否かなどで決まる
出力層の活性化関数では，通常は以下が用いられる

• 回帰 → 実数出力にしたいので恒等活性化関数
θ(u) = u

• 2クラスの確率論的分類 → シグモイド関数（ロジスティック関数）
θ(u) = Sigmoid(u)

• 多クラスの確率論的分類 → ソフトマックス関数
θ(u) = Softmax(u)
【補足】ソフトマックスのように，1変数関数ではないものも活性化関数と呼ぶ．

ニューラルネット＞基本の構成要素＞活性化関数の役割
活性化関数は省いてはいけない？
活性化関数がなければ，いくつ層を重ねても，結局1つの層に縮約できてしまう
W2(W1x + b1) + b2 = (W2W1)x + (W2b1 + b2)

つまり，活性化関数が無ければ，1層分の複雑さの関数しか表すことができない

線型（＋定数）よりも複雑な関数を表せるようにするのが活性化関数の役割

• ゆえに，活性化関数のことを「非線型性」（nonlinearity）と呼ぶこともある

ニューラルネット＞基本の構成要素＞ユニット
レイヤーを細かく見ると，ユニットを多数繋げたものという見方もできる
順伝播型NNは，ベクトルを変換するレイヤーを積み重ねたモデル

x

レイヤー レイヤー レイヤー レイヤー
（入力層） （中間層）（中間層）（出力層）

レイヤーは，複数のユニット（unit，素子）からなる

ニューラルネット＞基本の構成要素＞ユニット
ユニットは，レイヤーをさらに詳しく見たもの
重み行列 W，バイアス b，活性化関数 θ からなるレイヤーは，

W=

⋅
w1

𝔼 ，b =
⋅
wr

b1
𝔼
br

とすると，θ(Wx + b) =

⋅
θ(w1 x + b1)

𝔼
⋅
θ(wr x + br)

これは，次の形のユニット（unit，素子）を r 個並べたものともみなせる

• w を重み(weight)，b をバイアス(bias)，θ : 𝒟 × 𝒟 を活性化関数として
x Σ θ (w x + b)
⋅

【用語】「重み」はw ⋅ x = w1x1 + Π + wp xpという重み付き和の重みであることから．「バイアス（偏り）」は，出力の値を +b だけ「ずらす」ことから．

ニューラルネット＞表現図式と用語＞ユニット
ユニットを表す図式
ユニット x Σ θ (w ⋅ x + b) の図（重み w ，バイアス b ，活性化関数 θ ）

• 入力を受け取るとユニットの「現在の値」＝活性値（activation）が決まる
x1 w1

w2
x2
w3

θ (w ⋅ x + b )

x3

ユニットが計算結果の値を保持
→ 他のユニットの入力になる

ユニットの活性値は，次のユニットたちに渡される（重み付き和で）
u1

w1

h1

u2
u3

u

w2

h2

h

⋅
h1
θ(w1 u + b1)
=
[h2] [θ(w ⋅u + b )]
2
2

ニューラルネット＞表現図式と用語＞用語
ユニットを複数並べたものがレイヤーとなる
並べた個数はレイヤーの幅（width）という
幅が3のレイヤー

重みとバイアス
（weights and biases）

ニューラルネット＞表現図式と用語＞全体
よく使われる図式
入力層

隠れ層（中間層）

出力層（最終層）

x1
x2

隠れ層が2層ある → 隠れ2層のFNN
（two-hidden-layer FNN）という

入力層を除いて3層ある → 3層FNN
（three-layer FNN）ともいう

ニューラルネット＞アーキテクチャー
ユニット単位で見れば，結合の仕方はもっと自由に設計してもよい
今回扱ったレイヤーは，前の層の全てのユニットから，今の層の全てのユニット
に線が繋がっているので，全結合層（fully-connected layer）と呼ばれる

全結合

例えば，次のようなネットワークを考えてもよい

あえて一部だけ結合

ネットワークの設計のパターンはアーキテクチャ（architecture）と呼ばれる

ニューラルネット＞モデルの構造
ニューラルネットワークの人気の理由の一つは，その柔軟さにある
応用分野ごとに，問題に合った形のモデルを生み出すことができる

• 画像に適した構造，テキストに適した構造，……が研究・開発されている
応用分野ごとの適したアーキテクチャ選定は，学習における事前知識の典型例

ニューラルネットワークの最適化① 確率的勾配法

ニューラルネット＞最適化
NNの最適化に関する話を解説
ニューラルネットワークは，構造の自由度などから有力なモデルの一つ

しかし，パラメーターが複雑に絡み合うので，最適化は独自の工夫が凝らされる

• 確率的勾配法
• 誤差逆伝播
• という基本の考え方2つを中心に解説

ニューラルネット＞最適化＞取扱いの難点①
データも大規模になりがち
複雑なデータに対応するために，NNのパラメーター数は大規模になる

• パラメーターが多いので，訓練データも大規模にしないと性能が良くならない
インターネット時代の到来により，大規模な訓練データが収集されるように

• 2009年のImageNet：画像データが320万枚（その後1400万枚以上に拡張）
• 2023年のReﬁnedWeb：テキストデータが数千億単語（数兆単語のデータのう
ち一部だけが公開）

【参考】Deng, J., Dong, W., Socher, R., Li, L.-J., Li, K., & Fei-Fei, L. (2009). ImageNet: A large-scale hierarchical image database. 2009 IEEE Conference on

Computer Vision and Pattern Recognition, 248‒255. https://doi.org/10.1109/CVPR.2009.5206848
【参考】Penedo, G., Malartic, Q., Hesslow, D., Cojocaru, R., Cappelli, A., Alobeidli, H., Pannier, B., Almazrouei, E., & Launay, J. (2023). The ReﬁnedWeb dataset
for Falcon LLM: Outperforming curated corpora with web data, and web data only (arXiv:2306.01116). arXiv. https://doi.org/10.48550/arXiv.2306.01116

ニューラルネット＞最適化
基本的には勾配法ベースの手法で最適化を行う
基本的に大域的最適解を求める解の公式はないので，勾配法で最適化する

• 復習）勾配降下法のパラメーター更新式
θ ̂ ℙ θ ̂ ⋯ α Ω ⇒L(θ)̂
ただし，ニューラルネットワークの性質に合わせて，経験則や理論解析に基づく
様々な工夫が取り込まれている

• 単なる勾配降下法ではなく，かなり複雑な最適化手法が発達
• 最適化手法に取り入れる工夫も，帰納バイアスの一端を担う

ニューラルネット＞最適化＞大規模化への対応
NNの学習に使うデータは大規模 → 訓練時の計算量を制御する工夫が必要
経験リスクに基づく目的関数
n

1
L(θ) =
ϕ((xi, yi), gθ)
n∑
i=1
これの勾配は
n

1
⇒L(θ) =
⇒ϕ((xi, yi), gθ)
n∑
i=1
例えば10億件のデータがあると，1回の更新だけでも10億件の勾配計算

• あまりに多い場合，そもそもコンピューター1台には入りきらず実行不可能

ニューラルネット＞最適化＞大規模化への対応
リスクのサンプル近似のために，必ずしも全てのデータを用いる必要はない
ミニバッチ確率的最適化（mini-batch stochastic optimization method）

• 毎ステップ，データの一部をミニバッチ（mini-batch）として取得して学習
• バッチサイズ（batch size）＝ミニバッチ内のデータ数 | B | は1件〜数百件
• ミニバッチ B 上での訓練損失を小さくするように学習
1
L̃(θ) =
ϕ((xi, yi), gθ)
| B | (x∑
,y )∼B
i i

通常のミニバッチの作り方

• 訓練データをシャッフルし，バッチサイズごとに分割しておく
【用語】ミニバッチではなく全部の訓練用データを一度に使う，と言いたい場合には「フルバッチ学習」や「バッチ学習」ということがある．

ニューラルネット＞最適化＞ミニバッチ確率的勾配法
ミニバッチ確率的勾配降下法（mini-batch stochastic gradient descent）
パラメーター θ を適当に初期化（θ ̂ ℙ θ）
以下を必要なだけ繰り返す（1周をエポックと呼ぶ）

• 以下をすべてのミニバッチ B について実行する
̂
̂
θ
⇒
L̃(
θ)
B
ミニバッチ
で作った目的関数
の，現在の
における勾配
を計算
L̃
‣

‣ 勾配の反対方向にパラメーターを少し更新（学習率 α > 0）
θ ̂ ℙ θ ̂ ⋯ α ⇒ L̃(θ)̂

• 早期停止条件を満たしていたら中断
最終的な θ ̂ を使用する
【用語】エポック（epoch）は，同じ訓練データを何回学習に使ったかを数えたものとも言える．1エポック内では，各データ点はちょうど1回学習に使われる．

ニューラルネット＞最適化＞ミニバッチ確率的勾配法
通常の勾配法に加えて，次のような項目を決める必要がある
パラメーター θ を適当に初期化（θ ̂ ℙ θ）

何エポック学習するか？

以下を必要なだけ繰り返す（1周をエポックと呼ぶ）

• データをシャッフルし，ミニバッチ B1, B2, … に分割

バッチサイズ | B | は？

エポック毎にシャッフルと
ミニバッチ再分割をするか？

• 以下をすべてのミニバッチ B1, B2, … について実行する
̂
̂
ミニバッチ
で作った目的関数
の，現在の
における勾配
を計算
B
θ
⇒
L̃(
θ)
L̃
‣
̂ ℙ θ ̂ ⋯ α ⇒ L̃(θ)̂ （学習率 α > 0）
θ
勾配の反対方向にパラメーターを少し更新
‣

• 早期停止条件を満たしていたら中断
最終的な θ ̂ を使用する

【用語】エポック（epoch）は，同じ訓練データを何回学習に使ったかを数えたものとも言える．1エポック内では，各データ点はちょうど1回学習に使われる．

ニューラルネット＞最適化＞取扱いの難点②
目的関数の形状が（線型モデルなどと比較して）かなり複雑
線型モデルの最小二乗法など

ニューラルネットなど

凸最適化問題

非凸最適化問題

（大雑把に言って）解は一つで，
見つけやすい

局所的最適解が多数あり，大域的
最適解を求めるのが困難

θ

θ

局所的最適化手法
勾配法では基本的に「今いる場所の情報」だけでパラメーターを更新
見える情報

̂
θ

t

θ

現在地での勾配情報だけを頼りに
パラメーターを更新

【補足】全体像は，あらゆるパラメーター値を試してその時の損失を記録していけば把握可能だが，高次元のパラメーターでそれは現実的には実行不可能．
【補足】1次元だと，このように一旦上がって下がる必要があるなど，一段と難しい．高次元では逆に経路があって最適化しやすいこともある．
【補足】モメンタム法と呼ばれる工夫を取り入れた勾配法などでは，現在地だけでなく，それまでの勾配情報の履歴も加味して更新する．

局所的最適化手法
勾配法では基本的に「今いる場所の情報」だけでパラメーターを更新
全体像（見えない）

θ
損失値が低い θ に移動したい

ニューラルネット＞最適化＞複雑な構造を想定した工夫
実際には，色々な工夫を組み込んだ勾配ベースの最適化法が使われる
学習率の調整や，慣性項（momentum）を更新則に組み込むなどの工夫
AdamやMuonやCosine Annealing Scheduleなどの工夫がある（詳細は略）

【参考】Kingma, D. P., & Ba, J. (2015). Adam: A method for stochastic optimization. Proceedings of 3rd International Conference for Learning Representations.
ICLR 2015, San Diego.【用語】Adamという名前は，適応的なモーメント推定（adaptive moment estimation）に由来．
【参考】Defazio, A., Yang, X.A., Khaled, A., Mishchenko, K., Mehta, H., Cutkosky, A., 2024. The road less scheduled. Presented at the The Thirty-eighth Annual
Conference on Neural Information Processing Systems.

ニューラルネット＞最適化＞まとめ
ニューラルネットワークの最適化は，複雑で様々な工夫が必要
主な複雑さ

• データ量が大規模
• 目的関数の”地形”が複雑（局所的最適解が存在するなど）
主な解決方針

• 確率的勾配法により一度に全データを扱わなくてよい
• 局所的最適解を避けるための様々な工夫を凝らした最適化手法が開発
（実用上はその時点で評判が良いものを選ぶのがよい）

【補足】線型モデルと異なり局所的最適解が存在するなどの違いがあることは確かだが，近年では，パラメーターを大量に持つニューラルネットワークの目的関数は
逆に良い性質を持つのではないか，などの観点から研究が進んでいる（そうでないと，実用上，高い性能を発揮するモデルが実際に得られている≒最適化が実践的に
は上手くできていることの説明がつかない，という面がある）．

ロードマップ
第I部

第II部

?
ニューラル
ネットワーク
分類タスク
予測タスクの定式化
経験リスク最小化
線型モデル

?

正則化
過適合

モデル選択
学習の哲学
近似ターゲット

第7回
手嶋毅志 (Takeshi Teshima)

質問と回答（第7回 2025）

加点対象ではないその他の質問・コメント
授業が進むにつれて使われている式が複雑になってきたように感じて、少し難しいと思ったの
で復習したいと思います。

そうですね．一度分かってしまえば簡単に見えてくるのではないかと思うので，ぜひ資料を見
ながら復習して頂ければと思います．なお，数式の複雑さは現在より大きくは上がりません．

今ゼミで回帰モデルについて学んでいる最中[…]、今日の内容である、確率論的多値分類などは
ゼミでは学習していないことなのですがこの授業で学んでとても興味深いと感じました。

そういった感想，嬉しいです！ゼミで回帰を学ばれているときは，目的とするタスクが予測で
はなく何らかの分析という場合もあります．ぜひタスクを問いながら学んでみてください．
【補足】このページは加点対象ではありません．

加点対象ではないその他の質問・コメント
ソフトマックス関数が出力した数値を確率として扱うことは可能ですか。

確率値であるための要件は単に「0以上1以下であること」，確率値ベクトルなら「各成分が0

以上で足して1であること」です．故に，1成分だけ取り出した Softmax(z)[k] は，確率値です．

よく使われる活性化関数において、それぞれどういった特徴がありどういったものに使うのに
適していますか。

前回の資料の通りで，最終層では欲しい出力値の要件（実数？確率値？確率値ベクトル？）に
応じて選ぶことが多いです．現在は，中間層ではReLUやその亜種が使われることが多いです．
【補足】このページは加点対象ではありません．

加点対象の質問
分野ごとに適したニューラルネットワークの研究は、どこまで進んでいるのですか。

第II部でいくらか出てきます．一本道ではないので現在も様々な探索の試みがあります
が，おおよそ1970年代から現在までの研究の蓄積でかなり進んでいます．
画 像 で は C N N 系 や V i s i o n Tr a n s f o r m e r 系 が さ ま ざま ． テ キ ス ト で は R N N 系 や
Transformer系が発展．音声でもCNN系，RNN系，Transformer系が探索されていま
す．グラフニューラルネットというものや，可逆ニューラルネットという特殊な性質
を持ったものもあります．拡散モデルと呼ばれるアーキテクチャや，エンコーダー・デ
コーダー型というアーキテクチャ，推薦系ならTwo-tower型というアーキテクチャも
よく使われます．ちなみに順伝播型NNはこれらとよく併用されます．
【補足】このページは加点対象です．

加点対象の質問
（第2回のリアクションペーパーでの質問）過適合現象を説明したスライド64ページ
目のグラフを見ると、10次のときがTest Lossが一番小さく良いように見えるのです
が、授業で紹介された4次方が良いのでしょうか。10次だと複雑すぎるのですか？

左図についてのご質問ですね（よく見ていますね！）．10次での様子は右図です．
Test Lossの数値のとおり，実際に10次で問題ないです．

【補足】このページは加点対象です．

加点対象の質問
数ヶ月ほど前にGeminiが「死んでください」と言ったり、最近では画像生成AIを使っ
てわいせつな画像を作成し販売していた人が逮捕されたりとAI関連のニュースがあり
ました。なぜこのようなことが起こるのかを説明してほしいです。人間が無理にAIに
出力させようとしているようですが、出力する前に不適切な言葉や画像を出力しない
ように制御するシステムはないのですか？

いずれも生成系タスクを解くAIの，社会的影響に関しての話題ですね．

「リアルな生成ができること Θ 社会的規範や社会的通念を認識していること」なので
起こります．なお，どちらの事例も，問題か否かは社会的規範に依存します．
どうやって社会的規範を遵守させるかは，完全には解決していない課題ですが，改善
は進められています．詳しくは第13回で扱います．
【補足】このページは加点対象です．

加点対象の質問
勾配降下法における早期停止の利点は何ですか。

①時間の節約
事前に決めたステップ数だけ律儀に更新しなくても，モデルの改善がみられなく
なってきたらアルゴリズムを止めてしまってもいいですね．時間の節約になります．
②正則化
ある程度パラメーターの多いモデルなら，ステップを踏むほど学習が進み，いずれ
過適合し始めます（経験則）．勾配降下法の更新回数は（ざっくり）モデルの複雑さ
に対応します．ここで，早期停止条件として検証損失などの「モデル選択規準」に基
づくルールを用いれば，結果として正則化の効果が得られます．
【補足】このページは加点対象です．

出席確認

本日の出席確認番号は……

2025

告知
小テストは月曜日に公開します
モバイル出席のリアクションペーパーシステムは運用していません（今回から設
定をオフにしました）．

• これまでに出席のほうのシステムに送っていただいたリアクションペーパー
は，目を通しています．これまでに送っていただいた分の加点・無加点の判定
は，LETUSのほうと同じように扱います．（これまでの回での回答で結果的に
ほぼカバーできていました）

• 引き続き，質問はLETUSの「質問投稿フォーム」のほうへお願いします．

ロードマップ
第I部

第II部

階層性
深層学習
表現学習
非構造化データ
?
正則化
過適合

予測タスクの定式化
経験リスク最小化

学習の哲学

線型モデル

近似ターゲット

モデル選択

ニューラルネットワークの最適化② 誤差逆伝播

ニューラルネット＞最適化
基本的には勾配法ベースの手法で最適化を行う
基本的に大域的最適解を求める解の公式はないので，勾配ベースの方法で最適化
θ ̂ ℙ θ ̂ ⋯ α Ω ⇒L(θ)̂
これには，目的関数のパラメーターに関する勾配を計算する必要がある

• 例）ソフトマックス + 負の対数尤度の場合
⇒ϕ((x, y), gθ) = ⋯ ⇒sθ(x)[y] +

∑
k

gθ(x)[k] Ω ⇒sθ(x)[k]

• スコアリングモデルの出力成分 sθ(x)[k] の勾配 ⇒sθ(x)[k] を計算する必要がある

ニューラルネット＞最適化＞勾配計算
パラメーターが複雑に絡み合っているので，微分を愚直に計算したくはない
例えば3層のモデル（θ1 = θ2 = ReLU，θ3 = Softmax とする）なら，
gθ(x) = θ3(W3θ2(W2θ1(W1x + b1) + b2) + b3)
目的関数
n

1
L(θ) := ⋯
log gθ(x)[yi]
n∑
i=1
このとき，現在のパラメーターの具体的な値 θ = (W1, b1, W2, b2, W3, b3) における，
L(θ) の W1, b1, W2, b2, W3, b3 のそれぞれに関する微分（勾配）を計算せよ．
【注意】この問題は解かなくてよい．

ニューラルネット＞最適化＞勾配計算の例
手計算できる例で説明する
1次元特徴量での回帰を考える．

• モデルは次の順伝播型NNとする
g(w,b)(x) = ϵ(wx + b)

• データは1点(x, y)だけとして，目的関数は二乗誤差とする
2

L(w, b) = (y ⋯ g(w,b)(x))
勾配 ⇒L(w, b) を計算せよ．

【補足】ϵはシグモイド関数．入力層の活性化関数は線型活性化関数．

ニューラルネット＞最適化＞勾配計算の例＞手計算
試しに L(w, b) = (y ⋯ ϵ(wx + b)) の w による微分の計算手順を考えてみる
2

まず外側に2乗があるので，(x 2)′ = 2x を使って，
(y ⋯ ϵ(wx + b))

2

≠

2(y ⋯ ϵ(wx + b)) Ω (中身の(y ⋯ ϵ(wx + b))の微分)

続いて中身の (y ⋯ ϵ(wx + b)) を w で微分するから，
(y ⋯ ϵ(wx + b))

≠

⋯ϵ′(wx + b) Ω x のようになって，

残る ϵ′(wx + b) の部分は，確か ϵ′(u) = ϵ(u)(1 ⋯ ϵ(u)) だったから，まとめると
⋮L(w, b)
= ⋯ 2(y ⋯ ϵ(wx + b))ϵ(wx + b)(1 ⋯ ϵ(wx + b))x
⋮w
b による微分も同じように，……（少し大変）

ニューラルネット＞最適化
パラメーターが多段階で変換に絡むため，勾配の式は複雑になる
しかし，実はシステマチックに勾配を計算する方法がある

• それは，入力 x から損失値が出てくるまでの計算順をまず考えて，その後に，
それを遡りながら微分を計算するという方法

ニューラルネット＞最適化＞機械的に勾配を計算する方法
まずは，入力から損失の値が出てくるまでの計算手順を図にする
モデルと目的関数の計算式は次のとおりだが……
L(w, b) = (y ⋯ g(w,b)(x))2

（ここで g(w,b)(x) = ϵ(wx + b)）

途中の計算結果の値にも次のように記号を割り振っておいて……

s = wx + b を計算し， g = ϵ(s) を計算し，L = (y ⋯ g) を計算
2

計算手順の通りに，入力やパラメーターから損失値 L が決まるまでを図にする

w
x
b

s

y
g

L

ニューラルネット＞最適化＞機械的に勾配を計算する方法
この計算順を描いた図の「矢印」を逆順にたどりながら微分を計算する
⋮L
例えば g × L の矢印では，L の g での偏微分
= 2(y ⋯ g) を計算（他も同様）
⋮g

w
x
b

⋮s
=x
③
⋮w

⋮s
=1
④
⋮b

s

⋮g
②
= ϵ′(s)
⋮s

y
g

微分の計算手順

L

⋮L
= 2(y ⋯ g)
①
⋮g

⋮L
⋮L ⋮g ⋮s
⋮L ⋮L ⋮g ⋮s
⋮L ⋮g
=
=
最後に掛け算で
，
を得る（
は再利用）
⋮w
⋮g ⋮s ⋮w
⋮b
⋮g ⋮s ⋮b
⋮g ⋮s

ニューラルネット＞最適化＞計算グラフと自動微分
もっと複雑なニューラルネットでも全く同じことができる
このようにパラメーターやデータの変数を，計算の依存関係に従って矢印で繋い
だ図を，計算グラフ（computation graph）という．

一般の計算グラフでも，全く同じように，システマチックに勾配計算をできる

• これを自動微分（automatic diﬀerentiation）という
• 全結合層である必要もなく，どんなアーキテクチャでも勾配を計算できる
• 今では「微分可能な計算グラフ」の全般がニューラルネットと呼ばれる
【補足】どんな構造でも勾配をシステマチックに導出できるとはいえ，その勾配の計算効率が良いかどうかは別の話．
【補足】計算グラフとは，変数を頂点とする非巡回有向グラフで，各頂点に親の変数を引数とする関数が割り当てられているもの．なお，非巡回有向グラフとは，頂
点と有向辺の集まりで，周回できる道がないもののこと．

ニューラルネット＞最適化＞誤差逆伝播法（Backpropagation）
自動微分によって ⇒L(θ) を出力層側から順に遡りながら算出
入力層

隠れ層

θ1

⇒θ1 L(θ)

xi

出力層

θ2
θ3

計算グラフを構築しながら現在の値 θ で L(θ) を一度計算

⇒θ2 L(θ) = ⇒L(θ)
⇒θ3 L(θ)

出力層側のパラメーターから逆順に遡りながら算出

⇒θ1 L(θ)

⇒θ2 L(θ)

⇒θ3 L(θ)

【用語】損失関数≒誤差の情報を，入力側の層にめがけて伝えているように見えるので，誤差逆伝播法という．

yi∈
L(θ)

loss

ϕ(yi, yi∈ )

ニューラルネット＞実験＞設定
多クラス分類をニューラルネットで学習
タスク・学習戦略

モデル

5クラス分類・教師付き学習
特徴量

ラベル

x1
x2

y1
y2

𝔼

gθ(x)：順伝播型NN

目的関数

最適化手法

負の対数尤度

Adam

n
1
L(θ) := n Ξi=1 ϕ((xi, yi), gθ)

（慣性項を用いる確率的
勾配法の一種）

𝔼

n i.i.d.
{(Xi, Yi)}i=1 ∑ p

• 各層のユニット数は以下．

• バッチサイズは 32

‣ 入力層：2（入力次元）

• モデル選択基準は検証損失値
（早期停止＝学習エポック数
のみ選択）

‣ 隠れ層①：64

• L2正則化はなし

• エポック数上限 100

‣ 隠れ層②：32
‣ 出力層：5（クラス数）

• 学習率 α は 10

⋯3

• 早期停止（直近10エポック
の改善幅が0以下で停止）

• 活性化関数はReLU，最終層
• 初期値 θ ̂ はHeの初期化
のみSoftmax．
【実験ソースコード】[番号]̲neural̲network̲training.ipynb 【用語】Heの初期化は，何恺明（Hé Kǎimíng）先生の名前に由来．
訓練データ

ニューラルネット＞実験＞結果
訓練前の初期化した時点での予測器と，訓練後の予測器

訓練前のモデルと訓練標本

【実験ソースコード】[番号]̲neural̲network̲training.ipynb

訓練誤差

訓練後のモデルと訓練標本

ニューラルネット＞実験＞学習過程
予測器としても良さそうなものになっている
:= φ(gθ(x))
学習後のモデルに決定規則を合成した予測器 fθ(x)
の様子
̂
̂

分類器としての出力
（スコア最大値を与えるラベルを予測）

評価用データでの予測と正解ラベル

【実験ソースコード】[番号]̲neural̲network̲training.ipynb 【補足】決定規則には，最大事後確率則の φ( p) = arg maxk pk を用いた．

ニューラルネット＞まとめ
ニューラルネットは階層性と構造の自由度がポイント．最適化は大変．
ニューラルネットワークというモデルは，非線形モデルの主要なものの一つであ
り，実用的にも広く使われている．

• 順伝播型NNは，線型変換→非線型変換→……を積み重ねて複雑な関数を表現
• 自由度の高さを活かして様々なアーキテクチャーのNNが開発されている
最適化は誤差逆伝播法の考え方を使えば，複雑なアーキテクチャーのNNでも汎用
的に勾配を計算できて，勾配ベースの最適化手法が適用できる

• ただし，ニューラルネットの最適化は高難易度で，様々な工夫を要する
• 実際には，最新時点で評判の良い最適化手法を調べて使うことから

これまでのあらすじ
ニューラルネットワークという柔軟な道具を手に入れた

x

変換

変換

Π

変換

学習可能パラメーター

• 最終層の手前までを特徴写像 ϕ だと思えば，特徴量変換も含めて一気通貫
で学習可能なモデルともいえる

• 対して線型モデルでは，ϕ で特徴量に変換 → θ を掛けて出力 → θ ϕ(x)
⋅

⋅

これは人類にとって重要な道具になった．一体なぜ重要になったのか？

